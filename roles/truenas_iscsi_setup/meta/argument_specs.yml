---
argument_specs:
  main:
    short_description: "Configure iSCSI targets and extents"
    options:
      truenas_iscsi_setup_host:
        description: Target host address
        type: str
        required: true
      truenas_iscsi_setup_username:
        description: Authentication username
        type: str
      truenas_iscsi_setup_password:
        description: Authentication password
        type: str
      truenas_iscsi_setup_api_key:
        description: API key for authentication
        type: str
      truenas_iscsi_setup_validate_certs:
        description: Validate SSL certificates
        type: bool
        default: true
      truenas_iscsi_setup_state:
        description: Desired state
        type: str
        default: present
        choices: [present, absent]
