---
truenas_api_url: "{{ lookup('env', 'TRUENAS_API_URL') }}"
truenas_api_key: "{{ lookup('env', 'TRUENAS_API_KEY') }}"
truenas_validate_certs: false
# WARNING: Pool tests require spare disks. Set these to available disk names.
test_pool_name: integration_test_pool
test_pool_disks:
  - sdc
  - sdd
