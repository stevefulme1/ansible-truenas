---
argument_specs:
  main:
    short_description: "Set up alerts and SNMP monitoring"
    options:
      truenas_monitoring_host:
        description: Target host address
        type: str
        required: true
      truenas_monitoring_username:
        description: Authentication username
        type: str
      truenas_monitoring_password:
        description: Authentication password
        type: str
      truenas_monitoring_api_key:
        description: API key for authentication
        type: str
      truenas_monitoring_validate_certs:
        description: Validate SSL certificates
        type: bool
        default: true
      truenas_monitoring_state:
        description: Desired state
        type: str
        default: present
        choices: [present, absent]
